# config/extraction_schemas

Schema definitions used by OCR structured extraction.

## Contents
- `default.json`: baseline field schema.

## Notes
- Keep schema changes backward-compatible with OCR pipeline consumers.
